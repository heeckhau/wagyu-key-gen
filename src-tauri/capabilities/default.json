{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the Wagyu Key Gen main window: folder picker, clipboard write, reveal file in the file manager.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "clipboard-manager:allow-write-text",
    "opener:allow-reveal-item-in-dir"
  ]
}
